// ... අනෙක් requires සහ client නිර්මාණය කිරීම

const client = new Client({
    authStrategy: new LocalAuth({
        clientId: "my-bot-session"
    }),
    puppeteer: {
        args: [
            '--no-sandbox',
            '--disable-setuid-sandbox',
            // ✅ නව විකල්ප: සම්පත් භාවිතය අඩු කිරීම
            '--disable-gpu',
            '--disable-features=IsolateOrigins,site-per-process',
            '--no-zygote' // වේගවත් ආරම්භයක් සඳහා
        ]
    }
});

// 2. සම්බන්ධතාවය සාර්ථක වූ විට
client.on('ready', () => {
    console.log('✅ සාර්ථකයි! බොට් එක සම්බන්ධ වී ක්‍රියාත්මක වේ.');
});

// 3. පණිවිඩ හැසිරවීම
client.on('message', message => {
	// 💡 විධාන හැසිරවීම
    if(message.body === '!hello') {
        message.reply('Hello! මම දැන් whatsapp-web.js මගින් ක්‍රියාත්මකයි.');
    }
});

// 🚀 බොට් එක ආරම්භ කිරීම
client.initialize();